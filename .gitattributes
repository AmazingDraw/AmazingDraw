# Shell 脚本强制 LF：防止 Windows git clone（core.autocrlf=true）把
# install.sh 转成 CRLF 导致 bash 报 \r: command not found 全线崩溃。
*.sh        text eol=lf
*.py        text eol=lf
*.md        text eol=lf
# 二进制不转换
*.bin       binary
*.so        binary
*.pyd       binary
*.zip       binary
